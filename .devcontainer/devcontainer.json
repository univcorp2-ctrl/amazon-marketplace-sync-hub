{
  "name": "amazon-marketplace-sync-hub",
  "build": {"dockerfile": "../Dockerfile", "context": ".."},
  "forwardPorts": [8000],
  "postCreateCommand": "pip install -e '.[dev]'",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "charliermarsh.ruff"]
    }
  }
}
